// escape_characters.cpp
// Print a message to the console that contains escape characters.

#include <iostream>

int main() {
  std::cout << "This (\") is a quote, and this (\\) is a backslash."
    << std::endl;
  return 0;
}
